name: pemesanan_makanan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # ==== FIREBASE CORE ====
  firebase_core: ^2.30.0
  
  # ==== FIRESTORE ====
  cloud_firestore: ^4.15.0  # Versi lebih stabil
  
  # ==== WEB SUPPORT (WAJIB untuk Flutter Web) ====
  firebase_core_web: ^2.4.1
  cloud_firestore_web: ^3.5.0
  
  # ==== LOCAL STORAGE ====
  shared_preferences: ^2.5.0
  shared_preferences_web: ^2.2.0  # Web support

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/